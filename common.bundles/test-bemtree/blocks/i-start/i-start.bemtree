match(!this.data).mode('')(function() {
    return apply('', {
        data : {
            bundleName : 'test-bemtree',
            pageTitle  : 'BEMTREE test page'
        },
        ctx : {
            block : 'page',
            mods  : this.ctx.err? { error : 'not-found' } : {}
        }
    })
});
